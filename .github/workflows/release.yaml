---
name: release 

on:
  push:
    branches:
      - master
    paths:
      - src/**
      - Cargo.toml
      - Cargo.lock
      - action.yaml

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3
      - run: cargo build --release

      - uses: o0th/get-version@0.2.1
      - run: echo "VERSION=$(get-version)" >> $GITHUB_ENV

      - uses: o0th/create-tag@0.2.0
      - run: |
        username=$(echo "${{ github.repository }}" | cut -d '/' -f1)
        repository=$(echo "${{ github.repository }}" | cut -d '/' -f2)
        version=${{ env.VERSION }}
        sha=${{ github.sha }}
        create-tag ${{ github.repository }} $repository $version $sha

      - uses: ncipollo/release-action@v1
        with:
          tag: ${{ env.VERSION }}
          name: "check-version ${{ env.VERSION }}"
          bodyFile: ".github/changelogs/${{ env.VERSION }}.md"
          artifacts: "./target/release/check-version"
